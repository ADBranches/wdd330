// js/ui/savedTripsView.js
export function initSavedTripsView() {
  // At this stage, nothing dynamic yet.
  // Later, we'll read from localStorage and populate a list
  // of saved trips here.
}

export function showSavedTripsView() {
  // Will be used later to refresh list when we navigate to Saved view.
}
